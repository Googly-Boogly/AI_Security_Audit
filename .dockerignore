# Secrets never belong in the image — the key is passed at run time.
.env

# Local environments and caches
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / VCS
.idea/
.vscode/
.git/
.gitignore

# Run output — these are mounted as volumes, not baked in
generated/
workspace/
research/
briefings/

# Docker's own files
Dockerfile
.dockerignore
compose.yaml
